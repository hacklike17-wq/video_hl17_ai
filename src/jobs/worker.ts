// Phase 1 placeholder — worker entry point.
// Phase 4 sẽ thêm BullMQ workers cho voice/avatar/broll/assemble/publish.
console.log("[worker] Phase 1 — no jobs registered yet. Stub running.");
setInterval(() => {}, 1 << 30);
